import { StyleSheet } from "react-native";
import UserRepo from "../components/userrepos/UserRepo";

/**
 * login styles.
 */
const UserRepoStyle = StyleSheet.create({
  repo_parent: {
    alignItems: "center",
    justifyContent: "center",
    marginTop: 20
  }
});

export default UserRepoStyle;
